'''
sorted(iterable, key=None, reverse=False) -> a new list containing all items from the iterable in ascending order.
reversed(sequence) -> reverse iterator over values of the sequence
sum(iterable, start=0, /) -> the sum of a 'start' value (default: 0) plus an iterable of numbers
'''
