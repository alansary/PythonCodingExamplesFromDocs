#!/usr/bin/python3
def main():
    pass
    '''
        The OOP paradigm:
            - Organizes data into objects and functionality into methods (processing the data).
            - Defines object specification (data and methods) in classes.
        Object: a unit of data (attributes), of a particular class or type, with associated functionality (methods).
        OOP THREE PILLARS:
            - Encapsulation
            - Inheritance
            - Polymorphism
        Modules: files that contain python code VS. Classes: python code
            import module
                module.variable & module.function() & module.class()
            import module as abbreviation
                abbreviation.variable & abbreviation.function() & abbreviation.class()
            from module import variable/function/class, ..., variable/function/class
                variable & function() & class()
        Standard Case Convention For Classes:
            Start With Upper Case Letter.
        Supplementary Module Installs:
            yaml for data serialization
                pip search yaml
                pip install pyyaml
                pip install pyyaml --upgrade
                sudo apt-get install python-yaml
                sudo apt-get install yum
                sudo yum install python-yaml
            pytest for testing
                pip install -U pytest
                py.test --version
    '''
if __name__ == "__main__": main()
